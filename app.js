let sb,session,products=[],cart={},profile,wallet;
const app=document.getElementById('app');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=v=>`${Number(v||0).toFixed(2)} د.ل`;
async function boot(){
 try{sb=await loadSupabase();const r=await sb.auth.getSession();session=r.data.session||null;session?await loadStore():renderLogin();}
 catch(e){app.innerHTML=`<div class="card" style="text-align:center;margin-top:30px"><h1>⚡ Libyan Store</h1><p class="muted">${esc(e.message)}</p></div>`}
}
function renderLogin(msg=''){app.innerHTML=`<div style="max-width:430px;margin:35px auto;text-align:center"><div style="font-size:48px">⚡</div><h1>Libyan Store</h1><p class="muted">اشتراكات رقمية ومحفظة وتسليم أكواد</p><div class="card" style="display:grid;gap:10px;text-align:right"><input id="email" class="field" type="email" placeholder="البريد الإلكتروني"><input id="pass" class="field" type="password" placeholder="كلمة المرور"><button class="btn primary" id="login">دخول</button><button class="btn" id="signup">إنشاء حساب</button><small id="msg" class="muted">${esc(msg)}</small></div></div>`;login.onclick=()=>auth(false);signup.onclick=()=>auth(true)}
async function auth(signup){msg.textContent='جارٍ التنفيذ...';const email=emailInput(),password=document.getElementById('pass').value;if(password.length<6)return msg.textContent='كلمة المرور يجب أن تكون 6 أحرف على الأقل';const r=signup?await sb.auth.signUp({email,password}):await sb.auth.signInWithPassword({email,password});if(r.error)return msg.textContent=r.error.message;if(signup&&!r.data.session)return msg.textContent='تم إنشاء الحساب. تحقق من بريدك ثم سجّل الدخول.';session=r.data.session;await loadStore()}
function emailInput(){return document.getElementById('email').value.trim()}
async function loadStore(){const uid=session.user.id;const [p,w,pr]=await Promise.all([sb.from('profiles').select('full_name,phone,role').eq('id',uid).single(),sb.from('wallets').select('balance').eq('user_id',uid).single(),sb.from('products').select('id,name,description,category,price,currency').eq('active',true).order('created_at')]);profile=p.data;wallet=w.data;products=pr.data||[];renderStore()}
function renderStore(){app.innerHTML=`<div class="shell"><header class="topbar"><div><b style="font-size:22px">⚡ Libyan Store</b><div class="muted">${esc(profile?.full_name||session.user.email)}</div></div><button class="btn" id="logout">خروج</button></header><section class="card" style="margin:15px 0;background:#24150f;border-color:#5a3020"><span class="accent">رصيد المحفظة</span><strong style="display:block;font-size:30px;margin-top:4px">${money(wallet?.balance)}</strong></section><div id="view"></div><nav class="nav"><div class="nav-inner"><button class="active" data-view="store">🛍️ المتجر</button><button data-view="wallet">💰 المحفظة</button><button data-view="orders">📦 طلباتي</button>${profile?.role==='admin'?'<button data-view="admin">⚙️ الإدارة</button>':''}</div></nav></div>`;logout.onclick=async()=>{await sb.auth.signOut();session=null;renderLogin()};document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-view]').forEach(x=>x.classList.remove('active'));b.classList.add('active');({store:renderProducts,wallet:renderWallet,orders:renderOrders,admin:renderAdmin}[b.dataset.view])()});renderProducts()}
function renderProducts(){view.innerHTML=`<div class="topbar"><h2>المنتجات</h2><span class="pill">${products.length} منتج</span></div><div class="grid">${products.map(p=>`<article class="card"><span class="pill">${esc(p.category||'رقمي')}</span><h3>${esc(p.name)}</h3><p class="muted">${esc(p.description||'')}</p><strong class="accent">${money(p.price)}</strong><button class="btn light" style="display:block;width:100%;margin-top:12px" data-add="${p.id}">أضف للسلة</button></article>`).join('')}</div><div id="cartBox" style="margin-top:15px"></div>`;document.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>{cart[b.dataset.add]=(cart[b.dataset.add]||0)+1;renderCart()});renderCart()}
function renderCart(){const entries=Object.entries(cart);if(!entries.length){cartBox.innerHTML='<div class="card muted">السلة فارغة</div>';return}let total=0;const rows=entries.map(([id,q])=>{const p=products.find(x=>x.id===id);if(!p)return '';total+=Number(p.price)*q;return `<div style="display:flex;justify-content:space-between;padding:8px 0"><span>${esc(p.name)} × ${q}</span><b>${money(p.price*q)}</b></div>`}).join('');cartBox.innerHTML=`<div class="card"><h3>🛒 السلة</h3>${rows}<hr style="border-color:#263345"><b>الإجمالي: ${money(total)}</b><button class="btn primary" id="buy" style="width:100%;margin-top:12px">شراء من المحفظة</button></div>`;buy.onclick=checkout}
async function checkout(){const name=prompt('اسم العميل')||profile?.full_name||'';const phone=prompt('رقم الهاتف')||profile?.phone||'';if(!name||!phone)return;const items=Object.entries(cart).map(([product_id,quantity])=>({product_id,quantity}));const r=await sb.rpc('create_wallet_order',{p_items:items,p_customer_name:name,p_customer_phone:phone});if(r.error)return alert(r.error.message);cart={};alert('تم إنشاء الطلب بنجاح');await loadStore();}
async function renderWallet(){const r=await sb.from('wallet_topups').select('id,amount,method,status,created_at').eq('user_id',session.user.id).order('created_at',{ascending:false}).limit(20);const settings=await sb.from('store_settings').select('key,value').in('key',['libyana_number','almadar_number']);const nums=Object.fromEntries((settings.data||[]).map(x=>[x.key,x.value]));view.innerHTML=`<div class="card"><h2>💰 شحن المحفظة</h2><p class="muted">حوّل المبلغ إلى الرقم الظاهر ثم أرسل الطلب للمراجعة.</p><p>ليبيانا: <b>${esc(nums.libyana_number||'غير محدد')}</b><br>المدار: <b>${esc(nums.almadar_number||'غير محدد')}</b></p><select id="tm" class="field"><option value="libyana">ليبيانا</option><option value="almadar">المدار</option></select><input id="ta" class="field" style="margin-top:8px" type="number" min="1" placeholder="المبلغ بالدينار"><input id="tp" class="field" style="margin-top:8px" placeholder="رقم الهاتف المحوّل منه"><button class="btn primary" id="sendTop" style="width:100%;margin-top:10px">إرسال طلب الشحن</button></div><div class="card" style="margin-top:12px"><h3>طلبات الشحن</h3>${(r.data||[]).map(x=>`<div style="padding:9px 0;border-bottom:1px solid #202b3a">${money(x.amount)} — ${esc(x.method)} — <span class="pill">${esc(x.status)}</span></div>`).join('')||'<span class="muted">لا توجد طلبات</span>'}</div>`;sendTop.onclick=async()=>{const amount=Number(ta.value);if(!(amount>0)||!tp.value.trim())return alert('أكمل البيانات');const x=await sb.rpc('create_wallet_topup',{p_amount:amount,p_method:tm.value,p_sender_phone:tp.value.trim()});if(x.error)return alert(x.error.message);alert('تم إرسال طلب الشحن');renderWallet();}}
async function renderOrders(){const r=await sb.from('orders').select('id,total,status,created_at').eq('user_id',session.user.id).order('created_at',{ascending:false}).limit(30);let html='';for(const o of r.data||[]){const c=await sb.rpc('get_my_order_codes',{p_order_id:o.id});html+=`<article class="card" style="margin-bottom:10px"><div class="topbar"><b>${money(o.total)}</b><span class="pill">${esc(o.status)}</span></div><div class="muted">${new Date(o.created_at).toLocaleString('ar-LY')}</div>${(c.data||[]).map(x=>`<div style="margin-top:8px;padding:10px;background:#0a1017;border-radius:9px;font-family:monospace">${esc(x.code)}</div>`).join('')}</article>`}view.innerHTML=`<h2>📦 طلباتي</h2>${html||'<div class="card muted">لا توجد طلبات بعد.</div>'}`}
async function renderAdmin(){if(profile?.role!=='admin')return;const [pr,top,orders,codes]=await Promise.all([sb.from('products').select('*').order('created_at',{ascending:false}),sb.from('wallet_topups').select('*').order('created_at',{ascending:false}).limit(50),sb.from('orders').select('*').order('created_at',{ascending:false}).limit(50),sb.from('product_codes').select('id,product_id,code,status,order_id').order('created_at',{ascending:false}).limit(100)]);view.innerHTML=`<h2>⚙️ لوحة الإدارة</h2><div class="grid"><div class="card"><h3>المنتجات</h3>${(pr.data||[]).map(p=>`<div style="padding:8px 0;border-bottom:1px solid #202b3a"><b>${esc(p.name)}</b><br>${money(p.price)} — ${p.active?'نشط':'متوقف'}</div>`).join('')||'<span class="muted">لا توجد منتجات</span>'}</div><div class="card"><h3>طلبات الشحن</h3>${(top.data||[]).map(t=>`<div style="padding:8px 0;border-bottom:1px solid #202b3a">${money(t.amount)} — ${esc(t.method)} — <span class="pill">${esc(t.status)}</span>${t.status==='pending'?`<div style="margin-top:6px"><button class="btn primary" data-approve="${t.id}">اعتماد</button> <button class="btn" data-reject="${t.id}">رفض</button></div>`:''}</div>`).join('')||'<span class="muted">لا توجد طلبات</span>'}</div><div class="card"><h3>الأكواد</h3><p class="muted">عدد الأكواد: ${(codes.data||[]).length}</p>${(codes.data||[]).slice(0,20).map(c=>`<div style="padding:6px 0;font-family:monospace">${esc(c.code)} — ${esc(c.status)}</div>`).join('')}</div><div class="card"><h3>الطلبات</h3>${(orders.data||[]).map(o=>`<div style="padding:8px 0;border-bottom:1px solid #202b3a">${money(o.total)} — ${esc(o.status)}<br><span class="muted">${esc(o.customer_phone||'')}</span></div>`).join('')||'<span class="muted">لا توجد طلبات</span>'}</div></div>`;document.querySelectorAll('[data-approve]').forEach(b=>b.onclick=()=>reviewTopup(b.dataset.approve,true));document.querySelectorAll('[data-reject]').forEach(b=>b.onclick=()=>reviewTopup(b.dataset.reject,false))}
async function reviewTopup(id,approve){const note=prompt(approve?'ملاحظة الاعتماد (اختياري)':'سبب الرفض (اختياري)')||'';const r=await sb.rpc('review_wallet_topup',{p_topup_id:id,p_approve:approve,p_note:note});if(r.error)return alert(r.error.message);renderAdmin()}
boot();