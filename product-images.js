function productImage(name=''){const n=String(name).toLowerCase();if(n.includes('netflix')||n.includes('نتفلكس')||n.includes('نتفليكس'))return 'assets/netflix.svg';if(n.includes('shahid')||n.includes('شاهد'))return 'assets/shahid.svg';if(n.includes('spotify')||n.includes('سبوتيفاي'))return 'assets/spotify.svg';return ''}
function renderProducts(){el('view').innerHTML=`<div class="topbar"><h2>المنتجات</h2><span class="pill">${products.length} منتج</span></div><div class="grid">${products.map(p=>{const img=productImage(p.name);return `<article class="card product-card">${img?`<div class="product-media"><img src="${img}" alt="${esc(p.name)}" loading="lazy" decoding="async"></div>`:''}<div class="product-content"><span class="pill">${esc(p.category||'رقمي')}</span><h3>${esc(p.name)}</h3><p class="muted">${esc(p.description||'')}</p><div class="product-buy"><strong class="accent">${money(p.price)}</strong><button class="btn light" data-add="${p.id}">أضف للسلة</button></div></div></article>`}).join('')}</div><div id="cartBox" style="margin-top:15px"></div>`;document.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>{cart[b.dataset.add]=(cart[b.dataset.add]||0)+1;renderCart()});renderCart()}
